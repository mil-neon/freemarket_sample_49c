.singup3__container
  = render 'singup-header'
  %main.singup3__main
    .singup3__container
      %h2.singup3__head
        電話番号の確認
      .singup3__inner
        .singup3__content
          = form_with url: root_path, html: { class: "singup3__main__box__form" } do |f|
            .form-group
              = f.label :携帯電話の番号
              = f.text_field :tel, autocomplete: "tel", placeholder: "携帯電話の番号を入力", class: "input-default", autofocus: true
              本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。

            .actions
              = f.submit "SMSを送信する", class: "btn-sms"
              ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
            .text-right
              = link_to root_path, :target => "_blank" do
                電話番号の確認が必要な理由
                %i.icon-arrow-right
  = render 'buyers/buyer-footer'

